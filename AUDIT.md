# AUDIT.md — fabrioza.com SEO/AEO/GEO Overhaul, Phase 0 (Discovery)

Date: 2026-07-26 · Audited tree: `dist/` @ commit `297ea7c` (branch `main`, deployed & live)
Scope: read-only audit. No files changed except this document.

> **Context note:** a large technical-SEO remediation was already completed on this
> site during June–July 2026 (canonical convention, server-level 301s, structured-data
> offers fix, sitemap rebuild, GSC validation). Several problems this brief assumes to
> exist are therefore **already fixed**; each section below states current reality with
> evidence, and flags what is genuinely still open. Open items are marked **[OPEN]**,
> resolved ones **[DONE]**, partially resolved **[PARTIAL]**.

---

## 1. Stack identification

| Aspect | Finding |
|---|---|
| Framework | **Hybrid static-first site — no CMS, no SSG, no build step on this machine.** The repo IS the deployable artifact (`dist/`). |
| SPA part | React/Vite single-page app serving exactly two routes: `/` and `/privacy`. Bundle: `dist/assets/index-C6-wz3bb.js` (pre-built; **the Vite source project is not on this machine** — the bundle is binary-patched directly when needed). |
| Static part | ~103 prerendered pages as `folder/index.html` (collections, kits, landing pages, 65 blog posts). Each page's `<head>` is **hand-authored HTML in that file** — there is no template engine and no frontmatter; YAML-style `canonical:` fields do not exist in this stack. |
| Source of truth for `<head>` | **Per-page**: each `dist/<page>/index.html` owns its own head. Homepage head = `dist/index.html` (also carries the sitewide Organization/LocalBusiness/FAQ schema). There is no shared partial — this is the root cause of the canonical drift found in §6. |
| Templating | None at runtime. New pages are generated by Python scripts in the session scratchpad (`fab_article_template.py` lineage) and committed as static HTML. |

**Implication for later phases:** "refactor into a shared canonical function/partial" cannot be done as a runtime template change. The equivalent is (a) a **lint/fix script** run over `dist/**/index.html` (idempotent, CI-runnable), and (b) fixing the page-generator scripts.

## 2. Routing & URL model

| Aspect | Finding |
|---|---|
| Router | Apache (`php:8.3-apache` Docker image), rules in `dist/.htaccess` — **mod_rewrite only**. mod_alias (`Redirect`/`RedirectMatch`) is deliberately banned (dead code in `.htaccess` context here; documented in the file header). |
| Canonical URL convention | **No trailing slash, no `index.html` suffix** (root `/` excepted). Already enforced server-side. |
| Trailing-slash handling **[DONE]** | `.htaccess` §8: every `/page/` → 301 → `/page` (root and `/api/` exempt). Verified in the 311-check test suite. |
| `/home` duplicate **[DONE]** | `.htaccess` §2: `/home` and `/home/` → 301 → `https://fabrioza.com/`. |
| Where a 301 lives | `dist/.htaccess`, single ordered `RewriteRule` chain (redirects before routing). This is the only redirect layer; the fronting nginx (`scalaro-nginx-1` on the VPS) only terminates SSL and proxies. |
| HTTPS/www | www→apex and http→https 301s present, `X-Forwarded-Proto`-aware (proxy-safe), localhost-exempt for testing. |
| 404 behaviour | No SPA catch-all (removed 2026-07-09 — was causing soft-404s). Unknown routes serve branded `404.html` with real 404 status. |

## 3. Build & deploy pipeline

| Aspect | Finding |
|---|---|
| Repo | `C:\Users\PCL\Downloads\fabrioza-deployment-v3` → GitHub `yaseenishfaqjan/fabriozaupdated-site-`. Single clean-history lineage (history purged 2026-07-09). |
| Branching | **All commits go directly to `main`** today. No branch protection. Adopting `feat/seo-overhaul-pN` branches per this brief is a process change starting Phase 1. |
| Deploy | VPS `209.145.55.76`: `cd /opt/fabrioza && git pull && docker compose up -d --build` (run by the owner; container `fabrioza-web`, host port 8085). No CI. |
| `sitemap.xml` / `robots.txt` | **Static, hand-maintained files** in `dist/` — nothing generates them; they are edited alongside content changes. |
| Caching | HTML: no-cache/must-revalidate (deploys visible immediately). JS/CSS: 1 month → **bundle patches require bumping `?v=` on the asset tags in `dist/index.html`** (standing rule). |
| Test harness | `scratchpad/full_site_test.sh` — 311 checks against the local container (all pages 200/non-blank, slash+`index.html` 301s, redirect targets, images-on-disk, sitemap, headers, gzip, API). This is the de-facto regression suite Phase 6 should formalize. |

## 4. Content inventory

**Totals:** 105 `index.html` pages (incl. root + 404) · 65 blog posts under `/blog/` · 75 URLs in sitemap (the ~30-page gap = deliberately unlisted twin posts and legacy stubs, see below).

Page classes: homepage (SPA) · collections hub `/what-we-make` (15 categories) · 12 collection/catalog pages · 4 sport-kit pages + 4 kit guides · 10 service landing pages (`custom-hoodie-manufacturer`, `private-label-…`, etc.) · 6 country pages (`clothing-manufacturer-{usa,uk,canada,australia,uae,germany}`) · utility (`pricing`, `moq-calculator`, `tech-pack-template`, `privacy`, `blog` hub) · 65 posts.

**(a) Trailing-slash duplicates — [DONE].** None reachable: server 301s all `/page/` variants. Sitemap contains zero trailing-slash URLs (verified).

**(b) `/home` duplicate — [DONE].** 301 in place (see §2).

**(c) `Expertowears` / borrowed copy — [DONE / no action].** `grep -ri expertowears dist` → **0 hits**. No other competitor-brand strings found.

**(d) Meta-refresh redirects — [PARTIAL, cosmetic].** 10 stub files on disk still contain `http-equiv="refresh"`:
`about/`, `contact/`, `moq/`, `products/`, and 6 legacy blog slugs (`how-to-find-clothing-manufacturer`, `oem-vs-odm-vs-private-label`, `sports-team-uniform-manufacturing-guide`, `clothing-manufacturing-costs-breakdown`, `how-to-start-clothing-brand-2025`, `sustainable-fashion-manufacturing-guide`).
**However all 10 routes are intercepted by server-level 301s** (`.htaccess` §4–5), so crawlers never receive the meta-refresh — the files are dead weight. Phase 1 action: delete the 10 stub folders (the 301 rules already cover them; test suite expects 301 on these routes and will still pass).

**(e) `/?s=` search template — does not exist.** The site has no search feature at all; nothing renders for `?s=` (query strings on `/` just serve the homepage). The only `?s=` reference sitewide is the phantom SearchAction target inside the JS bundle (§5). Consequence for Phase 1: nothing to `noindex`; add the `robots.txt` disallow as cheap insurance and kill the SearchAction reference.

## 5. Current schema audit

**Authoritative schema is static, in each page's `<head>`** (JSON-LD blocks):

- `dist/index.html`: Organization (with `sameAs`: FB/IG/LinkedIn/TikTok), LocalBusiness ×2 (Sialkot factory + McDonough GA office), FAQPage, 5 Product blocks with full AggregateOffer.
- Collection/kit/catalog pages: ItemList + per-product `Product` with `offers` (AggregateOffer: `lowPrice/highPrice/priceCurrency/offerCount/availability/priceValidUntil`), `image`, `description`, `brand`. **This stack passed GSC validation July 2026** (offerCount ×16 PASSED, availability ×8 PASSED; 172 valid items).
- Priceless category lists (e.g. `/sportswear-catalog`): plain `ListItem`s, intentionally NOT Product (this was the fix for the last 16 GSC criticals).
- Blog posts: Article/BlogPosting + BreadcrumbList on most; **author is the organization, not a named Person** → E-E-A-T gap, in scope for Phase 4.
- Guides/country pages: Service + FAQPage.

**Misconfigured `SearchAction` — [PARTIAL, defused but present].** The React bundle contains
`potentialAction:{"@type":"SearchAction",target:"https://fabrioza.com/?s={search_term_string}",…}`
inside its runtime JSON-LD injector. That injector was **disabled 2026-07-10** (script type renamed to `application/ld+json-disabled`, confirmed: the bundle contains only the disabled type string), so **no parser receives the `{search_term_string}` artifact today** — 0 hits in any static HTML. Phase 1 action: strip the SearchAction from the bundle string outright (site has no search, so per this brief's own rule it should be **removed, not fixed**) + re-apply on future bundle merges.

**Standing constraint:** never add `review`/`aggregateRating` (no real review data; fake ratings = spam-policy risk). The two yellow GSC warnings (172 items each) are intentionally permanent.

## 6. Canonical logic audit

Every one of the 105 pages has exactly one `rel="canonical"` (0 missing). Convention is no-slash — **but 7 pages violate it [OPEN, genuine bug]:**

| File | Canonical currently | Problem |
|---|---|---|
| `pricing/index.html` | `https://fabrioza.com/pricing/` | Trailing slash → points at a URL that 301s to `/pricing` |
| `blog/clothing-manufacturing-costs-guide/` | `…/blog/clothing-manufacturing-costs-guide/` | same |
| `blog/how-to-find-reliable-clothing-manufacturer/` | `…/how-to-find-reliable-clothing-manufacturer/` | same |
| `blog/how-to-start-clothing-brand/` | `…/how-to-start-clothing-brand/` | same |
| `blog/oem-odm-clothing-manufacturer/` | `…/oem-odm-clothing-manufacturer/` | same |
| `blog/sustainable-clothing-manufacturer-pakistan/` | `…/sustainable-clothing-manufacturer-pakistan/` | same |
| `blog/team-uniform-manufacturer/` | `…/team-uniform-manufacturer/` | same |

The 6 blog offenders are exactly the **consolidation-target pillars** (the pages legacy slugs 301 into), i.e. the highest-value posts on the site — worth fixing first in Phase 1. Root cause: no shared head-generation; these 7 predate the no-slash convention. Fix = one idempotent script pass + a check added to the test suite so drift can't recur.

Cannibalization inventory for Phase 3 (10 confirmed `{topic}` ⇄ `{topic}-pakistan` twin pairs, all self-canonical, twins deliberately excluded from sitemap):
`clothing-manufacturer-for-startups`, `clothing-manufacturer-uk-brands`, `custom-hoodie-manufacturer`, `custom-sportswear-manufacturer`, `gym-wear-manufacturer`, `how-to-find-clothing-manufacturer`*, `oem-odm-clothing-manufacturer`, `private-label-clothing-manufacturer`, `streetwear-manufacturer`, `team-uniform-manufacturer` — plus near-twins `clothing-manufacturing-costs-breakdown` vs `-2026` vs `-guide`, `what-is-moq-clothing` vs `-manufacturing`, `low-moq-clothing-manufacturer` vs `-50-pieces`, `fabric-gsm-guide` vs `-clothing-manufacturing`, `custom-clothing-manufacturer-pakistan` vs `-sialkot-pakistan`, `how-to-start-clothing-brand` vs `-manufacturer` vs `-2025`*. (*already a 301 stub.)
Note additional 3-way overlap: several blog twins also compete with same-name **top-level landing pages** (e.g. `/custom-hoodie-manufacturer` page + 2 blog twins).

## 7. MOQ string audit

Full grep of `MOQ`, `50 pieces`, `50 pcs`, `from 50`, `20 sets` across `dist/`:

- **96 HTML files mention MOQ**; **80 files / ~365 occurrences state the 50-piece position** (titles, meta descriptions, OG tags, H1/hero copy, FAQ answers, JSON-LD descriptions, `llms.txt`, `ai.txt`).
- **11 files already use the 15–20-set position** (sport-kit economy): `soccer-kits`, `football-kits`, `baseball-kits`, `basketball-kits`, the 4 kit guides, `sportswear-catalog`, `what-we-make` (kit cards), `pricing` (kit rows) — the brief's noted 50-vs-20 inconsistency, confirmed.
- Heaviest 50-piece files (occurrence count): `streetwear-collection` 33 · `mens-collection` 32 · `plus-size` 31 · `medical-scrubs` 29 · `kids-clothing` 29 · `blog/low-moq-clothing-manufacturer` 23 · `blog/low-moq-clothing-manufacturer-50-pieces` 23 (slug itself contains "50-pieces" → Phase 2 slug migration confirmed necessary; both twins exist already) · `intimate-apparel` 22 · `sustainable-fashion` 20 · homepage 17 · plus ~70 more files ≤19 each. Full machine-readable table can be regenerated any time with:
  `grep -rinc "50 pieces\|50 pcs\|50pcs" dist --include="*.html"`.
- Also affected beyond HTML: `llms.txt` (MOQ 50 in 4 places), `ai.txt`, homepage JSON-LD FAQ answers, and the SPA bundle's hero copy (bundle patch + `?v=` bump required).

## Extra findings (not in the 7-item brief, relevant to Phase 1)

1. **Footer copyright split:** 80 files `© 2026`, **4 files `© 2025`** (`premium-hoodies`, `premium-tshirts`, `puff-print-streetwear`, `pricing`) — the three newest zip-template pages + pricing. No shared footer partial exists; fix = script pass (dynamic year is impossible in static HTML without JS; recommend standardizing the text to 2026 and adding a one-line JS year-setter only where a `<script>` already exists).
2. **Certification claims audit (E-E-A-T / truthfulness):** ISO 9001 in 82 files (confirmed cert, per owner). But **BSCI (19 files), OEKO-TEX (20), WRAP (16), SEDEX (2), GOTS (4)** appear without confirmation. `# TODO(fabrioza):` owner must confirm which of BSCI/OEKO-TEX/WRAP/SEDEX/GOTS are actually held (with cert numbers) — unverifiable ones must be removed in Phase 1. `# TODO(fabrioza):` ISO 9001 certificate number for `hasCredential` schema (Phase 4).
3. **robots.txt** has no `Disallow: /*?s=` (harmless today — no search endpoint — add as insurance).
4. `Allow: /custom-*/` line in robots.txt uses a trailing slash pattern that matches nothing canonical (harmless; tidy in Phase 1).

## Phase 1 shortlist derived from this audit

1. Fix the 7 trailing-slash canonicals (script pass + regression check).
2. Delete the 10 dead meta-refresh stub folders (301 rules already cover the routes).
3. Strip the SearchAction/`{search_term_string}` string from the bundle (+ document re-apply rule).
4. robots.txt: add `Disallow: /?s=` and `Disallow: /*?s=`; tidy `Allow: /custom-*/`.
5. Normalize 4 footers to `© 2026`; remove unconfirmed cert claims per owner's answer to the TODO above.
6. Sitemap is already clean (75 canonical no-slash URLs) — add an automated check rather than changes.

**TODO(fabrioza) items for the owner:** ① confirm held certifications + numbers (BSCI / OEKO-TEX / WRAP / SEDEX / GOTS, ISO 9001 cert no.); ② confirm the 20-piece MOQ business decision and the 20-tier per-unit prices before Phase 2 (do NOT want to publish invented prices); ③ decide keep-vs-merge preference defaults for the twin posts ahead of Phase 3 review.

— End of Phase 0. No site files modified. Awaiting review before Phase 1.
